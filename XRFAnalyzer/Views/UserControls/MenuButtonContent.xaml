<UserControl x:Class="XRFAnalyzer.Views.UserControls.MenuButtonContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:XRFAnalyzer.Views.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">

    <DockPanel 
        VerticalAlignment="Center" 
        HorizontalAlignment="Left"
        >
        <materialDesign:PackIcon
            Style="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:MenuButtonContent}}, Path=PackIconStyle}"
            Kind="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:MenuButtonContent}}, Path=PackIconKind}"  />
        <Label
            Style="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:MenuButtonContent}}, Path=LabelStyle}"
            Content="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:MenuButtonContent}}, Path=LabelContent}"></Label>
    </DockPanel>
</UserControl>

